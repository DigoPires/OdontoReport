services:
  - type: web
    name: odontoreport
    env: node
    buildCommand: apt-get install -y chromium-browser || apt-get install -y chromium; npm install; npm run build
    startCommand: npm run start
    envVars:
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium-browser
